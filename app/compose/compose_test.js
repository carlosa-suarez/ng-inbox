'use strict';

describe('myApp.compose module', function() {

  beforeEach(module('inboxApp.compose'));

  describe('compose controller', function(){

    it('should ....', inject(function($controller) {
      //spec body
      var composeCtrl = $controller('ComposeCtrl');
      expect(composeCtrl).toBeDefined();
    }));

  });
});